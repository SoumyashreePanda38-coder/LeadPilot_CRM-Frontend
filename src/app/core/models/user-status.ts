/**
 * ==========================================
 * Enum : UserStatus
 *
 * Description:
 * Defines the current status of a user.
 * Must match the backend UserStatus enum.
 * ==========================================
 */

export enum UserStatus {
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE'
}