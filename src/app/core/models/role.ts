/**
 * ==========================================
 * Enum : Role
 *
 * Description:
 * Defines user roles in LeadPilot CRM.
 * Must match the backend Role enum.
 * ==========================================
 */

export enum Role {
  ADMIN = 'ADMIN',
  EXECUTIVE = 'EXECUTIVE'
}